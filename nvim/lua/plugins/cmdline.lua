local cmdline = require('fine-cmdline')

cmdline.setup({
    popup = {
        position = {
            row = '40%',
        }
    }
})
-- vim.api.nvim_set_keymap('n', ':', '<cmd>FineCmdline<CR>', {noremap = true})
